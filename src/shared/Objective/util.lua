--[[

    Objective Utility Module

    Griffin Dalby
    2025.10.28

    This module will provide some small utilities for the objective
    interface.

--]]

--]] Services
--]] Modules
local types = require(script.Parent.types)

--]] Sawdust
--]] Settings
--]] Constants
--]] Variables
--]] Functions
--]] Module
local obj_util = {}

--[[ SANITIZE ]]--
obj_util.sanitize = {}
obj_util.sanitize.settings_objective = function(conditions: types.ObjectiveSettings)
    
end
obj_util.sanitize.settings_condition = function(objectives: types.ConditionSettings)
    
end

return obj_util